{
  "name": "idolbooth-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.10"
  },
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "dev:node": "tsx watch src/index.ts",
    "build": "tsc --noEmit",
    "cf:deploy": "npm run build && wrangler deploy",
    "cf:deploy:full": "npm run build && wrangler d1 migrations apply batchloom-db --remote && wrangler deploy",
    "cf:r2:create": "wrangler r2 bucket create idolbooth-storage",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "d1:migrate": "npx wrangler d1 migrations apply batchloom-db --remote",
    "d1:migrate:local": "npx wrangler d1 migrations apply batchloom-db --local",
    "analyze:funnel": "tsx scripts/analyze-funnel.ts",
    "seed": "tsx src/db/seed/seed.ts",
    "generate-assets": "tsx scripts/generate-assets.ts"
  },
  "dependencies": {
    "drizzle-orm": "0.34.0",
    "hono": "4.6.3",
    "lucia": "3.2.0",
    "openai": "4.67.3",
    "pngjs": "7.0.0",
    "sharp": "0.33.5",
    "stripe": "17.0.0",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260508.1",
    "@eslint/js": "9.9.0",
    "@types/node": "22.5.5",
    "@types/pngjs": "6.0.5",
    "drizzle-kit": "0.25.0",
    "eslint": "9.9.0",
    "globals": "15.9.0",
    "miniflare": "^4.20260507.1",
    "prettier": "3.3.3",
    "tsx": "4.19.1",
    "typescript": "5.5.3",
    "typescript-eslint": "8.0.1",
    "vitest": "2.1.2",
    "wrangler": "^4.90.0"
  }
}
